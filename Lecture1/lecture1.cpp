#include <iostream>
#include <cmath>



using namespace std;

int main(){
	cout << "Hello World " << endl;
    int x = 10;
    int y = 6;
    cout << x && y;



   // this doesn't work int z = pi % 4;





	return 0;
}



//int range -2^31 -> 2^31-1
//1 byte has 8 bits 
// when you don't assign a value to a int variable it spits out a garbage number
// you can not use moduolus with any number with a decimal before-hand, it must be used with only whole numbers
//lazy evaluation: all expressions are evaluated left to right, if by evaluating one half of the expression,  
//the complier can arrive at the solutoin, the other half is never computed/evaluated. 


